export interface FrameInterval{
    label: string;
    percentage: number;
}

export interface MetaData{
    filename: string;
    duration: number;
}

export interface Result{
    filename: string;
    image_urls: string[];
}