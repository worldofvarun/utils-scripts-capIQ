export interface FrameInterval{
    label: string;
    percentage: number;
}

export interface MetaData{
    filepath: string;
    duration: number;
}

export interface Result{
    filepath: string;
    image_urls: string[];
}